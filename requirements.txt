# Python packages to install via pip
# Optimized for Raspberry Pi ARM architecture

# ONNX Runtime for ARM64
# Note: Don't pin exact version, let pip find compatible one
onnxruntime>=1.15.0

# NumPy - Will use system version but ensure minimum
numpy>=1.24.0